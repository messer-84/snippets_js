      if (Math.abs(nextSlide - currentSlide) == 1) {
        directionSlide = (nextSlide - currentSlide > 0) ? "right" : "left";
      }
      else {
        directionSlide = (nextSlide - currentSlide > 0) ? "left" : "right";
      }
